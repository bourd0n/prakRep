Строковые махинации

Time limit:	2 s
Memory limit:	128 M
Ваша программа должна оперировать с набором фильтров. Фильтр - это нечто, что получает на вход объект некоторого определенного типа и возвращает в качестве результата работы объект этого же типа. Например, практически в любом графическом редакторе есть фильтр "осветление картинки".

Каждый фильтр обладает уникальным именем. Ваша программа должна уметь работать со следующими фильтрами над строками (имя фильтра выделено):

eql - возвращает ту же строку, которую получил на вход;
upc - возвращает строку, полученную из исходной переводом каждого символа в верхний регистр;
dwn - возвращает строку, полученную из исходной удалением всех символов, которые были в верхнем регистре;
dup - возвращает строку, полученную дублированием исходной (например, строка "x1y" будет преобразована в строку "x1yx1y");
rev - возвращает строку, полученную обращением исходной строки (например, строка "x1y" будет преобразована в строку "y1x");
fst - возвращает строку, состоящую из того же множества символов, как и исходная, но все символы упомянуты 1 раз в порядке первого вхождения в исходную строку (например, строка "x1yxz" будет преобразована в строку "x1yz");
dpc - возвращает строку, полученную дублированием каждого символа исходной строки (например, строка "x1y" будет преобразована в строку "xx11yy");
cut - возвращает строку, полученную усечением исходной строки до 10 символов, если они там есть, в противном случае искомая строка совпадает с исходной.
Удобно оформлять каждый фильтр в виде отдельного класса, который является наследником абстрактного "фильтра". Абстрактный "фильтр" содержит метод для выполнения операции фильтра, принимающий единственный строковый аргумент и возвращающий строку. В классах-потомках этот метод переопределяется. Такая архитектура классов позволяет определить поведение каждого из фильтров и отдельно от остальных фильтров, и отдельно от логики работы с фильтрами. Это повышает понимание программы, упрощает добавление новых фильтров и т.п.

Ваша программа должна получать на вход со стандартного ввода две строки. В первой строке находится непустая последовательность имён фильтров, завершающаяся словом stop, во второй - слово (т.е. в этой строке нет пробельных символов). Ваша программа должна вычислить и вывести результат применения данного слова к данной последовательности фильтров, начиная с первого; выход i'го фильтра должен подаваться на вход (i+1)'го.

Каждый фильтр должен быть оформлен в виде отдельного класса (как это описано выше). В своём решении вам нельзя пользоваться операцией instanceof.

Обратите внимание, в ejudge посылка может содержить только один файл. Поэтому вам нужно будет поместить все классы в один файл, причем метод main() должен быть определен в публичном классе.

Examples

Input	Output
eql rev dup cut stop
gh593S
S395hgS395

